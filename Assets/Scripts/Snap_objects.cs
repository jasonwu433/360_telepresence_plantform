using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class Snap_objects : MonoBehaviour
{
    public snapObjectProperties snapObjectProperty;
    public enum snapObjectProperties {None, number, plus, minus, times, divide};
    // Start is called before the first frame update
    void Start()
    {
        
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
